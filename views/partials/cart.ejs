<div class="header-cart-container">
  <div class="header-cart" id="header-cart">
    <p class="cart-icon"></p>
    <span class="cart-count <%= userCartCount > 0 ? 'header-cart-active' : '' %>" id="cart-count"><%= userCartCount %></span>
  </div>
</div>