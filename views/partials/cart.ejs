<div class="header-cart-container">
  <div class="header-cart <%= userCart.totalCount > 0 ? 'header-cart-active' : '' %>" id="header-cart">
    <p class="cart-icon"></p>
    <span class="cart-count" id="cart-count"><%= userCart.totalCount || 0 %></span>
  </div>
</div>